---
description: 导航栏规范 - 全项目自定义导航，无标题
globs: src/**/*.vue, src/pages.json
alwaysApply: true
---

# 导航栏规范

## 全局配置

- [`src/pages.json`](src/pages.json) 的 `globalStyle` 必须设置 `"navigationStyle": "custom"`
- **禁止**在页面 `style` 或 `globalStyle` 中配置 `navigationBarTitleText`（不使用系统导航标题）

## 页面实现

- 每个页面的根组件使用 `<PageRoot>`（见 [page-root.mdc](page-root.mdc)），由它内部渲染 `<CustomNav />`
- **不要**在页面里再单独写 `<CustomNav />`
- **导航栏不显示标题文字**，仅保留状态栏占位；非首页自动显示返回按钮
- 需要强制隐藏/显示返回时，在 PageRoot 上使用 `:back="false"` 或 `:back="true"`

## 返回按钮

- 返回样式统一由 `CustomNav` 维护，按钮紧贴左上角
- 默认：**浅蓝主题底 + 主色箭头 +「返回」文字**
- 深色页：在 `PageRoot` 上加 `dark-nav`，使用半透明幽灵按钮（不抢暗色内容）
- **禁止**各页面自行实现返回按钮或使用系统默认返回样式

```vue
<template>
  <PageRoot>
    <!-- 页面内容 -->
  </PageRoot>
</template>
```

深色示例：

```vue
<template>
  <PageRoot dark-nav :extra-style="{ backgroundColor: '#0f1319' }">
    <!-- 暗色页面内容 -->
  </PageRoot>
</template>
```

## 禁止事项

- 不要在页面内重复写 `statusBarHeight` / `navBarHeight` 计算逻辑
- 不要在自定义导航栏内添加标题 `<text>`（返回按钮除外，由 CustomNav 统一提供）
- 不要改回系统默认导航栏（`navigationStyle: default`）
